#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
    int valor;
    printf("Digite um valor inteiro: ");
    scanf("%d", &valor);
    printf("Valor: %s!\n", valor);
    int notas = 0;
    printf("Quantidade de notas de R$100: ")
    return 0;
}
